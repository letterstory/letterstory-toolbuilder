### 2026-09-06: Redeployed main commit 4053030678403fc6d123777235d429def1c2e7f9 to Porter staging
**By:** Infra
**What:** Verified Porter CLI/project access, observed that local `porter apply -f porter.yaml -w` could not build here because this machine has no Docker daemon, then relied on the already-running authoritative GitHub Actions deploy for `main` (run `34048904132`) which built, pushed, and applied SHA `4053030678403fc6d123777235d429def1c2e7f9` successfully to `https://web-22301-57c6c7ab-4p0z458q.onporter.run`. Post-deploy verification confirmed the new `/build` UI is live by finding the newly added topbar `Theme` button in the hydrated page.
**Why:** The workflow is the established Porter deploy path in this repo and completed a successful rollout (`porter apply -f porter.scale-zero.yaml ... --wait` then `porter apply -f porter.yaml ... --wait`) for the requested SHA when local Docker-based apply was unavailable. I also smoke-tested `/api/tools/generate` against the live URL; it returned structured JSON and timed out at the app budget (`400` with `Tool generation took too long...`) rather than crashing on the read-only filesystem fallback, so the known `/app/.data` regression did not reappear, but branded generation performance still needs follow-up.
