### 2026-09-06: Verified main commit 820debd reached Porter staging via auto-triggered GitHub Actions deploy
**By:** Infra
**What:** Confirmed the `Deploy to letterstory-toolbuilder` GitHub Actions workflow auto-triggered on the `main` push for commit `820debd16b319af583673b29a3117f76b1740e9e` (run `34071687861`) and completed successfully. Verified `https://web-22301-57c6c7ab-4p0z458q.onporter.run/api/health` returned HTTP 200 with `{"ok":true,"service":"letterstory-toolbuilder",...}` and loaded the live app successfully at `/brand` with HTTP 200, returning the expected `Letterstory Toolbuilder` Next.js document shell plus current dashboard chunks. Because this Porter workflow deploys the image tagged with `$GITHUB_SHA`, the successful run for `820debd` is the deployed artifact now serving staging.
**Why:** This staging environment is updated by the push-to-`main` GitHub Actions Porter workflow, so the correct redeploy path was to watch the auto-triggered run for commit `820debd` finish, then validate both service health and a real page load from the live Porter URL.
