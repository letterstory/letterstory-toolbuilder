### 2026-09-05: Builder UI cleanup removed dead topbar controls and duplicate recent-tools chrome
**By:** Frontend
**What:** Removed the non-functional Home pill, decorative M avatar, duplicate focus-composer icon button, duplicate recent-tools icon button, misleading topbar refresh/more button, duplicate dashboard Recent tools section, disabled mic button, fake “Auto” pill, and the misleading paperclip normalization button in the composer. Renamed the embed action from “Copy with copy” to “Copy full snippet.”
**Why:** Mir's live review called out clutter and buttons that looked clickable but did nothing. Site URLs already normalize on blur and before submit, so the paperclip control added confusion without adding capability; removing both composer affordances leaves only real actions visible, and recent-tool switching stays in the existing top-left project pill dropdown instead of being repeated across the UI.
