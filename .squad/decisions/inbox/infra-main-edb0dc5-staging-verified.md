### 2026-09-06: Verified main commit edb0dc5 reached Porter staging via auto-triggered GitHub Actions deploy
**By:** Infra
**What:** Confirmed the `Deploy to letterstory-toolbuilder` GitHub Actions workflow auto-triggered on the `main` push for commit `edb0dc59da4488c4168ef0974aa9b2b2e85e5399` (run `34069952396`) and completed successfully. Then verified `https://web-22301-57c6c7ab-4p0z458q.onporter.run/api/health` returned a healthy JSON payload, loaded `/build` successfully, and confirmed the deployed Next.js bundle includes commit-specific strings from the `5b56856` feature work now present in merged head `edb0dc5`, including `Leave the tool name blank if you want us to title it.`, `No brand logo captured for this run.`, and `Exact asset` in `/_next/static/chunks/app/(dashboard)/build/page-0b799007feb5e220.js`.
**Why:** This repo’s Porter deploy is driven by the push-to-`main` GitHub Actions workflow, so the correct redeploy path was to watch the auto-triggered workflow complete and then verify the live staging app’s health endpoint plus a commit-specific frontend bundle marker on the hosted build page.
