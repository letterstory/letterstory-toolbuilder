### 2026-09-05: Fresh production logo-fidelity verification found two remaining generic-logo failures
**By:** Tester
**What:** I re-ran live production generation against `https://web-22301-57c6c7ab-4p0z458q.onporter.run` for `airbnb.com`, `mailchimp.com`, `spotify.com`, `doordash.com`, and `figma.com`, captured fresh Playwright screenshots under `docs/screenshots/logo-fidelity-verification/`, and confirmed that all five tools still passed generation plus the `/t/[id]` iframe contract. However, **Mailchimp and DoorDash still rendered generic/invented header marks instead of real brand logos**: Mailchimp used a circular envelope badge, and DoorDash used a serif `D` badge. Airbnb, Spotify, and Figma showed real or clearly recognizable brand logos.
**Why:** This means the recent logo/font-fidelity fixes improved some cases but have **not** fully eliminated the generic-logo failure mode. It is especially important that the backend `brandFidelity` advisory still reported `pass` for both Mailchimp and DoorDash despite the generic logo output, so the current automated advisory is over-permissive and should not be treated as authoritative proof of real-logo fidelity yet.
