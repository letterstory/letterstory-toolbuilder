### 2026-09-05: Porter rollout must avoid surge on the current cluster
**By:** Infra
**What:** `letterstory-toolbuilder` now rolls out in two Porter applies: first an exact `porter.scale-zero.yaml` revision with `instances: 0`, then the normal `porter.yaml` revision back at `instances: 1`. Both manifests include the live app name/domain so the exact applies are valid in CI.
**Why:** The failing revisions were not app crashes; Kubernetes never started the new pod. `kubectl describe`/events on the Porter cluster showed `FailedScheduling`, `NotTriggerScaleUp: max node group size reached`, and Karpenter `InsufficientCapacityError` / `VcpuLimitExceeded` while the rolling update tried to place a second pod alongside the old one. Scaling the app to zero first frees the only schedulable slot, and GitHub Actions run `33979004766` then completed successfully; Porter logs showed new revision `v49` starting (`▲ Next.js 15.5.25`, `✓ Ready in 195ms`), and `curl https://web-22301-57c6c7ab-4p0z458q.onporter.run/api/health` returned HTTP 200 at `2026-09-05T16:52:27Z`.
