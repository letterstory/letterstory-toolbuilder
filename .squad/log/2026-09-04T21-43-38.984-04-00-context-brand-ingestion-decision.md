# 2026-09-04T21:43:38.984-04:00 — Context brand ingestion decision

- Requested by: MirRaonaq
- Contributors: Lead, Fact Checker, Squad Coordinator, and Scribe.
- Mir requested integrating `letterstory/letterstory#1350`'s new Context.dev-based org-level brand ingestion into toolbuilder's tool-generation flow.
- Lead investigated the merged PR and recommended an additive compatibility layer in toolbuilder: accept optional upstream `brandContext` / `brandSnapshot` input, map it to toolbuilder's existing snapshot shape, prefer it when present, and keep Firecrawl as fallback.
- Fact Checker independently verified that PR #1350 is merged and found the "browser hydration + more accurate than Firecrawl" claim unverified for toolbuilder's current production workload; recommended feature-flag/parity testing rather than a hard swap.
- Squad Coordinator reviewed both reports and found them aligned.
- Outcome: the shared team recommendation is to preserve the freshly verified Firecrawl pipeline as fallback while pursuing an additive compatibility layer if approved.
- Status: implementation is pending Mir's go-ahead.
